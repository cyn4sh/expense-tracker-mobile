class SocketClient {
  // WebSocket connection logic will be implemented here once a
  // live-data feature (e.g., real-time budget updates) requires it.
  //
  // Planned responsibilities:
  // - Establish and maintain a WebSocket connection
  // - Expose a Stream for repositories to listen to
  // - Handle reconnect-on-disconnect logic
}